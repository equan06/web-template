import * as React from "react";
// implement a userProvider here, allowing access to the user's state (name, email, login, logout, etc)

export function UserProvider({props}) {
    const [user, setUser] = React.useState({
        
    });
    function loginWithCredential() {

    }

    function logout() {
        // invalidate session state
    }

    function createWithCredential() {
        
    }
}